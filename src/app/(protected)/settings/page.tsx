export default function SettingsPage() {
  return (
    <main className="min-h-screen bg-cream px-4 py-6">
      <section className="mx-auto max-w-3xl rounded-card bg-card p-6 shadow-card">
        <h1 className="font-display text-3xl text-text-primary">Settings</h1>
        <p className="mt-2 text-sm text-text-secondary">
          Preference settings panel will be expanded in subsequent iterations.
        </p>
      </section>
    </main>
  );
}
